const s = [5, 7, 2];
function editInPlace() {
  "use strict";

  s[0] = 2;
  s[1] = 5;
  s[2] = 7;

}
editInPlace();

// what a good example that showcases the use of index to access and override an arrays elements even though declared with const